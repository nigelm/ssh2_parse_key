# .prettierrc or .prettierrc.yaml
overrides:
    # markdown files should be wrapped with lower width
    - files: ["*.md"]
      options:
          proseWrap: "always"
          printWidth: 80
    - files: ["*.yaml"]
      options:
          proseWrap: "always"
          printWidth: 120
# end
